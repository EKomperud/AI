function [scores,traces] = CS4300_WW5(max_steps,f_name,boards)
%UNTITLED3 Summary of this function goes here
%   Detailed explanation goes here

len = length(boards);
scores = zeros(len);
traces = zeros(len);

for b = 1:len
    [score, trace] = CS4300_WW1(max_steps,f_name,boards
end


end

